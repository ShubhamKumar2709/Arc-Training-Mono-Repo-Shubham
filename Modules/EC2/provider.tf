terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 5.0" # Adjust the version as needed
    }
  }
}
terraform {
  required_version = ">= 1.3.0" # Adjust based on your Terraform version
}
